# ERP 고도화 작업 인덱스

> 최종 갱신: 2026-07-14 · FastAPI + SQLAlchemy 2.0 + Alembic + MySQL/SQLite · 프론트: Next.js(App Router)
> "마스터 + 원시 재고이동" 골격 → **거래 문서·원가·부가세·복식부기 회계까지 갖춘 실동작 ERP** 로 확장한 내역 정리.
> 모든 단계는 실행 검증(테스트/라이브 스모크) 완료.

---

## 0. 한눈에 보기

| 단계 | 내용 | 상태 |
|---|---|---|
| Phase 0 | 안전화 — Alembic 도입, 시크릿 가드, 재고 락(TOCTOU), PUT 부분수정 | ✅ |
| Phase 1 | 거래 문서 — 발주(PO)/수주(SO), 재고 잔고 캐시, 백필 | ✅ |
| #3 원가 | 이동평균 원가, 재고평가액, 매출총이익(COGS) | ✅ |
| #2 신뢰성 | pytest, Docker/compose, GitHub Actions CI | ✅ |
| 회계 | 결제/수금(AR/AP), 거래처 미수·미지급 잔액 | ✅ |
| 결제-문서 연결 | PO/SO별 지급·수금 매칭, 문서별 미지급/미수 | ✅ |
| 인덱스 | stock_movements(created_at, partner_id), audit_logs(created_at) | ✅ |
| 금액 정밀도 | 금액·원가 Decimal(NUMERIC) 전환 (float 제거) | ✅ |
| 부가세(VAT) | 공급가/세액 분리, `compute_tax` 스냅샷 | ✅ |
| 세금계산서 | 발행·취소 (발행 시점 스냅샷 문서) | ✅ |
| 반품 | 매입반품(PRET)·매출반품(SRET) — 음수 수량 역이동 | ✅ |
| 여신한도 | 거래처 credit_limit + 수주 확정 시 한도 체크 | ✅ |
| 다중창고 | 재고·이동평균을 `품목+창고` 차원으로 확장, 창고간 이전(TRANSFER) | ✅ (보류 해제) |
| 프론트 | 단일 HTML 콘솔 → **Next.js SPA 전환 완료** (레거시 HTML 제거) | ✅ |
| 총계정원장(GL) | 전기형 복식부기 — 분개장·계정별원장·시산표·손익·재무상태·재대사·재전기 | ✅ (S1~S6) |
| 하드닝 | httpOnly 세션 쿠키 인증, 대시보드 풀버전, 레거시 콘솔 제거 | ✅ |
| 인증 디벨롭 | 비밀번호 정책·강제변경, 세션 무효화(token_version), 2FA(TOTP), 비번 재설정(메일/임시비번), 가입 승인·거절, 프로필 수정 | ✅ (2026-07-14) |
| 기간 마감 | 월별 회계기간 + 마감선 이하 소급 기표 차단 + 마감 대체분개(→3110) + 마감 해제 | ✅ (2026-07-14) |

---

## 1. Alembic 마이그레이션 체인 (단일 head — 13개)

```
643a533a1b31  initial schema (기존 스키마 전체)
   → b7e1c0d2f3a4  stock_balances 캐시 + StockMovement.ref_type/ref_id (+ 백필)
   → 2b1bc2d1c4c0  purchase_orders / sales_orders (+ lines)
   → c3d4e5f6a7b8  이동평균 원가: StockBalance.avg_cost, StockMovement.cost (+ 백필)
   → d4e5f6a7b8c9  payments(AR/AP) + 리포팅 인덱스 3종
   → e5f6a7b8c9d0  payment.ref_type/ref_id (결제-문서 연결)
   → f6a7b8c9d0e1  decimal money (금액·원가 NUMERIC 전환)
   → a1b2c3d4e5f6  vat (공급가/세액 분리)
   → b2c3d4e5f6a7  tax_invoices (세금계산서)
   → d5e6f7a8b9c0  returns (매입/매출 반품)
   → e6f7a8b9c0d1  credit_limit (거래처 여신한도)
   → f7a8b9c0d1e2  multi_warehouse (창고 + 품목·창고별 잔고/이동평균)
   → b3f2a1c0d9e8  general_ledger (accounts / journal_entries / journal_lines + 12계정 시드)
   → c4d5e6f7a8b9  auth_hardening (users 확장 + password_reset_tokens, email UNIQUE)
   → d6e7f8a9b0c1  period_close (accounting_periods — 월별 회계기간 마감)  ← head
```

> `auth_hardening` 은 **기존 계정을 전부 비밀번호 강제 변경 대상으로 백필**한다(새 정책 소급 적용).
> 업그레이드 후 각 사용자는 첫 로그인에서 비밀번호를 바꿔야 업무 API 를 쓸 수 있다.

- 운영 DB: `alembic upgrade head` 후 시드. 로컬 SQLite: 기동 시 자동 생성(편의).
- env/URL은 `migrations/env.py` 가 앱 설정(`.env`)에서 읽음.

---

## 2. 도메인 모델 (`app/models.py`)

| 모델 | 용도 |
|---|---|
| `StockBalance` | **품목+창고**별 현재고 캐시(on_hand) + 이동평균원가(avg_cost). 원천은 stock_movements |
| `Warehouse` | 창고 마스터. 이동은 창고 귀속, 창고간 이전은 TRANSFER |
| `PurchaseOrder` / `PurchaseOrderLine` | 발주서 헤더/명세 (received_qty 누적) |
| `SalesOrder` / `SalesOrderLine` | 수주서 헤더/명세 (shipped_qty 누적) |
| `Payment` | 결제/수금(AP/AR), 선택적 문서 귀속(ref_type/ref_id) |
| `TaxInvoice` | 세금계산서 발행 스냅샷 (발행·취소) |
| `Account` / `JournalEntry` / `JournalLine` | 총계정원장 — 계정과목 12종 시드, 전표(원천당 1건 UNIQUE), 분개 라인(NUMERIC(18,4)) |
| (컬럼) `StockMovement.ref_type/ref_line_id/cost/warehouse_id/tax_amount` | 이동 출처·원가·창고·세액 |
| (컬럼) `Partner.credit_limit` | 여신한도 (수주 확정 시 체크) |

상태 머신 — PO: `draft→confirmed→partial→received / cancelled` · SO: `draft→confirmed→partial→shipped / cancelled`

---

## 3. API (`app/api/` — 라우터 15개)

| 라우터 | 주요 엔드포인트 |
|---|---|
| `purchase.py` | `GET/POST /api/purchase-orders`, `/{id}/confirm`, `/{id}/receive`(부분입고), `/{id}/return`, `/{id}/cancel`, `DELETE` |
| `sales.py` | `GET/POST /api/sales-orders`, `/{id}/confirm`, `/{id}/ship`(부분출고), `/{id}/return`, `/{id}/cancel`, `DELETE` (확정 시 여신한도 체크) |
| `warehouses.py` | 창고 CRUD + 창고간 이전(TRANSFER) |
| `costing.py` | `GET /api/costing/valuation`, `/valuation/summary`, `/margin` |
| `payments.py` | `GET/POST /api/payments`(ref 필터), `/{id}`, `DELETE` |
| `invoices.py` | 세금계산서 발행·취소·조회 |
| `gl.py` | `/api/gl/journal`, `/journal/{id}`, `/ledger`, `/trial-balance`, `/income-statement`, `/balance-sheet`, `/reconcile`, `POST /manual`(수기전표), `DELETE /journal/{id}`(수기만), `POST /rebuild`(재전기) |
| `partners.py` | 거래처 CRUD + `GET /api/partners/{id}/balance` (미지급/미수) |
| `stock.py` | 재고 잔고 기반(현재고 O(1)), 문서 이동 수기삭제 차단 |

핵심 서비스:
- `app/services.py`: `apply_stock_delta`(행 잠금·음수 방지·이동평균 갱신), `post_movement`/`post_return`/`post_transfer`, `compute_tax`, 채번.
- `app/gl.py`: 전기 서비스 — `post_for_movement` / `post_for_payment` / `delete_for_source` / `post_manual` / `rebuild_gl` / `ensure_accounts`(계정 시드).

---

## 3.5 인증·계정 (2026-07-14 디벨롭)

| 영역 | 내용 |
|---|---|
| 비밀번호 정책 | `PASSWORD_MIN_LENGTH`(10) + 영문·숫자 + 공백·아이디 포함·흔한 비번 금지(`app/security.py`). 서버가 단일 소스이고 화면은 `GET /api/auth/password-policy` 로 문구를 받아 쓴다 |
| 강제 변경 | 정책 미달·임시비번 계정은 `must_change_password` → `require_password_ok` 게이트가 업무 API 를 403 으로 막고, 프론트는 `/change-password` 로 유도 |
| 세션 무효화 | JWT 에 `token_version` 을 심고 비번 변경·재설정·임시비번·비활성화 시 증가 → 그 이전 토큰(다른 기기) 전부 무효 |
| 가입 승인/거절 | 가입 시 부서·사유 입력 → 관리자 승인(역할 부여) 또는 거절(`POST /api/users/{id}/reject`, 사유 기록·로그인 시 안내) |
| 비번 재설정 | SMTP 설정 시 1회용 링크 메일(`password_reset_tokens`, sha256 해시 저장, 기본 30분) / 미설정 시 관리자 임시비번 발급(`POST /api/users/{id}/temp-password`, 평문 1회 표시) |
| 2단계 인증 | TOTP(pyotp) — `/2fa/setup`(QR SVG) → `/2fa/enable`(코드 확인 후 활성) → 로그인 시 `otp` 필요. 분실 시 관리자가 `POST /api/users/{id}/2fa/disable` |
| 프로필 | 본인 이름·이메일·부서 수정(`PUT /api/auth/me`). 이메일은 UNIQUE(재설정 대상 식별) |
| 세션 연장 | `POST /api/auth/refresh` — 프론트가 30분마다 호출해 사용 중 만료를 막는다 |
| 무차별 대입 | 로그인 5회/5분(IP+아이디), 가입 10회/시간, 비번찾기 5회/시간 → 429 (인메모리) |

## 3.6 기간 마감 (2026-07-14)

| 항목 | 내용 |
|---|---|
| 단위 | 월별 회계기간(`accounting_periods`, `YYYY-MM`). 행은 지연 생성 |
| 차단 | **마감선(가장 최근 마감월) 이하 전면 동결** — 수기 전표·과거 날짜 결제·원천 삭제·재전기가 400 `period_closed`. 강제 지점은 `assert_period_open` 하나(모든 전기가 `post_journal`, 모든 삭제가 `delete_for_source` 를 지난다) |
| 마감분개 | 손익계정 상계 → 차액을 3110 이월이익잉여금으로(`CLOSING` 전표, 전표일자=그 달 말일) |
| 규칙 | 진행 중인 달은 마감 불가(그 달 영업이 막힌다) · 마감은 오래된 달부터 순차 · 해제는 최근분부터 역순 |
| 재무제표 | 손익계산서는 CLOSING 제외(마감해도 그 달 매출 그대로), 재무상태표는 포함(미마감 손익만 `net_income` 에 남음) |
| 동시성 | 기표=FOR SHARE / 마감·해제=FOR UPDATE 로 기간 행을 잡아 직렬화 |
| 감사 | `entity=gl_period` 로 마감·해제 기록. 해제는 삭제되는 마감 전표 스냅샷을 `before` 에 남김 |
| API | `GET /api/gl/periods` · `POST /api/gl/periods/{period}/close` · `/reopen` (payment:write) |

## 4. 권한 (RBAC)

`PERMISSIONS` 18종(`app/seed.py:26-45`) — `*`, `user:*`, `audit:read`, `partner:*`, `item:*`, `stock:*`, `purchase:*`, `sales:*`, `payment:*`, `invoice:*`.
역할: `admin`(*) / `manager` / `staff` / `warehouse` / `viewer`.

> GL 조회·기표는 **전용 권한을 신설하지 않고** `payment:read` / `payment:write` 를 재사용한다(회계 권한 묶음 유지).

---

## 5. 재고·원가·회계 흐름 요약

- **입고(발주 receive)**: IN 이동 → 창고별 잔고 증가 + 이동평균 갱신, 발주 received_qty 누적 → GL 전기(차 상품/부가세대급금, 대 외상매입금).
- **출고(수주 ship)**: OUT 이동 → 잔고 차감(부족 시 400·롤백), COGS = 출고수량 × 이동평균 → GL 전기(차 외상매출금/매출원가, 대 매출/부가세예수금/상품).
- **반품**: 음수 수량 역이동. 매입반품은 (매입단가−이동평균) 차이를 `5130 재고원가차이`로 흘려 재고 계정 == 재고평가액 불변식 유지.
- **창고간 이전(TRANSFER)**: 회사 전체 평가액 불변 → **GL 무전기**. 세금계산서도 무전기(이동 시점에 부가세 이미 인식).
- **재고평가액** = Σ(현재고 × 이동평균원가), **매출총이익** = 매출 − COGS.
- **미지급/미수** = 매입/매출 청구(이동에서 파생) − 결제(payments). 문서 귀속 시 문서별 산출.
- **GL 정합**: `GET /api/gl/reconcile` 이 GL 잔액 vs aging·valuation·margin 을 대조(diff≠0이면 `POST /api/gl/rebuild` 로 원천에서 전량 재전기).

---

## 6. 프론트엔드 (Next.js — `frontend/`)

레거시 단일 HTML 콘솔(`app/static/index.html`)은 **제거됨**(커밋 `fb5c14d`). 백엔드는 API 전용.

화면(App Router, `(app)` 그룹): `dashboard` · `partners` · `items` · `warehouses` · `stock` · `purchase` · `sales` ·
`transactions` · `valuation` · `aging` · `payments` · `gl` · `users` · `audit` · `me`
(+ 비보호 라우트 `login` · `forgot-password` · `reset-password` · `change-password`).

- `gl` 은 단일 페이지 5탭(시산표 · 분개장 · 계정별원장 · 손익계산서 · 재무상태표) + 수기 전표 모달.
- 인증은 httpOnly 세션 쿠키(커밋 `8a1a275`), 대시보드는 KPI·추이·재고부족·최근거래 풀버전(`8121bac`).
- `login` 은 로그인/회원가입 + 2FA OTP 단계, 비밀번호 강도 표시·아이디 중복 확인.
  `me` 는 프로필 수정·비밀번호 변경·2FA 설정(QR), `users` 는 승인/거절·임시비번 발급·2FA 해제.
- 확인이 필요한 동작은 **`useConfirm()`**(`components/ui/ConfirmProvider`)을 쓴다 — 딤 배경 + 카드 +
  취소/확정 모달을 Promise 로 감싸 `if (!(await confirm({...}))) return;` 형태로 호출한다.
  브라우저 기본 `window.confirm`/`alert` 은 **전 화면에서 제거**(2026-07-14, 15곳).

---

## 7. 신뢰성 (테스트 · 배포)

- **테스트**: `tests/` — 총 **107개** 테스트 함수.
  `test_api.py`(38: 인증/RBAC·부분수정·재고 가드·발주 입고·수주 출고·이동평균/마진·결제·세금계산서·반품·여신) ·
  `test_auth.py`(30: 비밀번호 정책·강제변경 게이트·세션 무효화·가입 승인/거절·재설정(메일/임시비번)·2FA·프로필) ·
  `test_gl.py`(28: 사건별 전기·멱등·삭제 동반·rebuild·수기전표·재무제표·권한) ·
  `test_warehouse.py`(6) · `test_concurrency.py`(5). `conftest.py` 로 임시 SQLite 격리.
  (시드 admin 은 정책 미달 비번이라 `must_change_password` 가 켜진 채 생성된다 — 업무 API 를 보는
  테스트는 `conftest.clear_must_change()` 로 플래그를 내리고, 게이트 자체는 `test_auth.py` 가 검증한다.)
- **Docker**: `Dockerfile`(alembic→seed→uvicorn), `docker-compose.yml`(MySQL8+API, 헬스체크·자동 마이그레이션), `.dockerignore`.
- **CI**: `.github/workflows/ci.yml` — push/PR마다 pytest.

---

## 8. 실행 방법

```powershell
# 로컬 SQLite (즉시)
.\run_sqlite.ps1                      # 시드+서버 → http://localhost:8000 (admin/admin1234)

# 프론트 (별도 터미널)
cd frontend; npm install; npm run dev  # http://localhost:3000

# MySQL 등 운영
alembic upgrade head
python -m app.seed
uvicorn app.main:app --port 8000

# Docker
docker compose up --build

# 테스트
pip install -r requirements-dev.txt
pytest

# GL 재전기(원천에서 전표 전량 재생성)
python -m scripts.rebuild_gl
```

---

## 9. 다음 후보

1. **전표일자 시간대** — 전표일자는 이동 `created_at`(DB 시각), 마감 판정은 UTC. DB가 UTC이고 사업장이 KST면 월 경계 하루가 인접 월로 기표될 수 있다(확인 필요 → 사업장 시간대 단일 소스 고정).
2. 세금계산서 기준 갭(`docs/gl-design.md` §9-5) — 계산서 스냅샷이 **주문 전량** 기준이라 부분출고 시 GL 부가세(납품 기준)와 갈릴 수 있음. 어느 쪽으로 닫을지 결정 필요.
3. 원천 삭제 정책(§9-4) — 물리 삭제 → 취소 플래그 + 역분개 전환 여부.
4. 결제 aging 리포트 확장(문서 부분결제 스케줄·만기).
5. `Payment.method`별 현금 계정 분리(§9-7) — 전표 재생성으로 소급 가능.

---

## 10. 파일 트리 (핵심)

```
erp/
├─ app/
│  ├─ config.py            시크릿 가드
│  ├─ models.py            마스터·문서·재고·결제·세금계산서·GL
│  ├─ schemas.py           Pydantic 스키마 (+ Page, ErrorOut)
│  ├─ services.py          apply_stock_delta / post_movement / post_return / post_transfer / compute_tax / 채번
│  ├─ gl.py                GL 전기 서비스 (사건 판별 → 분개 생성 → 차대 검증 → 전기/삭제/재전기)
│  ├─ security.py          비밀번호 해시·정책 + JWT(token_version) + 재설정 토큰 해시
│  ├─ mailer.py            SMTP 발송(선택) — 미설정이면 관리자 임시비번 경로로 폴백
│  ├─ errors.py            FieldError → {fields} 응답(라우터가 내는 필드 단위 오류)
│  ├─ seed.py              권한 18종·역할·관리자(정책 미달이면 강제변경 표시)·샘플 + 계정과목 12종
│  ├─ main.py              앱 팩토리 + 라우터 15개 + 오류 핸들러 (API 전용)
│  └─ api/                 auth users partners items stock warehouses purchase sales
│                          costing payments invoices gl stats reports audit
├─ frontend/               Next.js App Router (15화면 + 인증 4화면, httpOnly 쿠키 인증)
├─ migrations/             Alembic (env.py + versions/ 14개, head=c4d5e6f7a8b9)
├─ scripts/rebuild_gl.py   GL 전량 재전기
├─ tests/                  conftest + test_api(38) / test_auth(30) / test_gl(28) /
│                          test_warehouse(6) / test_concurrency(5)
├─ docs/                   gl-design.md (GL 설계·구현 결과) · nextjs-migration-plan.md · screenshots/
├─ Dockerfile  docker-compose.yml  .dockerignore
├─ .github/workflows/ci.yml
├─ pytest.ini  requirements.txt  requirements-dev.txt
└─ 고도화_작업내역.md       (이 문서)
```
