# ERP 고도화 작업 인덱스

> 최종 갱신: 2026-07-03 · FastAPI + SQLAlchemy 2.0 + Alembic + MySQL/SQLite
> "마스터 + 원시 재고이동" 골격 → **거래 문서·원가·회계까지 갖춘 실동작 ERP** 로 확장한 내역 정리.
> 모든 단계는 실행 검증(테스트/라이브 스모크) 완료.

---

## 0. 한눈에 보기

| 단계 | 내용 | 상태 |
|---|---|---|
| Phase 0 | 안전화 — Alembic 도입, 시크릿 가드, 재고 락(TOCTOU), PUT 부분수정 | ✅ |
| Phase 1 | 거래 문서 — 발주(PO)/수주(SO), 재고 잔고 캐시, 백필 | ✅ |
| #3 원가 | 이동평균 원가, 재고평가액, 매출총이익(COGS) | ✅ |
| #2 신뢰성 | pytest 12개, Docker/compose, GitHub Actions CI | ✅ |
| 회계 | 결제/수금(AR/AP), 거래처 미수·미지급 잔액 | ✅ |
| 결제-문서 연결 | PO/SO별 지급·수금 매칭, 문서별 미지급/미수 | ✅ |
| 인덱스 | stock_movements(created_at, partner_id), audit_logs(created_at) | ✅ |
| 프론트 콘솔 | 발주/수주/재고평가/결제 화면 (단일 HTML 확장) | ✅ |
| 다중창고 | (보류 — 사용자 결정) | ⏸ |

---

## 1. Alembic 마이그레이션 체인 (단일 head)

```
643a533a1b31  initial schema (기존 스키마 전체)
   → b7e1c0d2f3a4  stock_balances 캐시 + StockMovement.ref_type/ref_id (+ 백필)
   → 2b1bc2d1c4c0  purchase_orders / sales_orders (+ lines)
   → c3d4e5f6a7b8  이동평균 원가: StockBalance.avg_cost, StockMovement.cost (+ 백필)
   → d4e5f6a7b8c9  payments(AR/AP) + 리포팅 인덱스 3종
   → e5f6a7b8c9d0  payment.ref_type/ref_id (결제-문서 연결)   ← head
```

- 운영 DB: `alembic upgrade head` 후 시드. 로컬 SQLite: 기동 시 자동 생성(편의).
- env/URL은 `migrations/env.py` 가 앱 설정(`.env`)에서 읽음.

---

## 2. 신규 도메인 모델 (`app/models.py`)

| 모델 | 용도 |
|---|---|
| `StockBalance` | 품목별 현재고 캐시(on_hand) + 이동평균원가(avg_cost). 원천은 stock_movements |
| `PurchaseOrder` / `PurchaseOrderLine` | 발주서 헤더/명세 (received_qty 누적) |
| `SalesOrder` / `SalesOrderLine` | 수주서 헤더/명세 (shipped_qty 누적) |
| `Payment` | 결제/수금(AP/AR), 선택적 문서 귀속(ref_type/ref_id) |
| (컬럼 추가) `StockMovement.ref_type/ref_line_id/cost` | 이동 출처 추적 + 이동별 원가 |

상태 머신 — PO: `draft→confirmed→partial→received / cancelled` · SO: `draft→confirmed→partial→shipped / cancelled`

---

## 3. 신규/변경 API (`app/api/`)

| 라우터 | 주요 엔드포인트 |
|---|---|
| `purchase.py` | `GET/POST /api/purchase-orders`, `/{id}`, `/{id}/confirm`, `/{id}/receive`(부분입고), `/{id}/cancel`, `DELETE` |
| `sales.py` | `GET/POST /api/sales-orders`, `/{id}`, `/{id}/confirm`, `/{id}/ship`(부분출고), `/{id}/cancel`, `DELETE` |
| `costing.py` | `GET /api/costing/valuation`, `/valuation/summary`, `/margin` |
| `payments.py` | `GET/POST /api/payments`(ref 필터), `/{id}`, `DELETE` |
| `partners.py`(추가) | `GET /api/partners/{id}/balance` — 미지급/미수 잔액 |
| `stock.py`(개편) | 재고 잔고 기반으로 재작성(현재고 O(1)), 문서 이동 수기삭제 차단 |

핵심 서비스(`app/services.py`): `apply_stock_delta`(행 잠금·음수 방지·이동평균 갱신), `post_movement`(입출고 단일 진입점).

---

## 4. 권한 (RBAC) 추가

`purchase:read/write`, `sales:read/write`, `payment:read/write` — 시드(`app/seed.py`)의 권한·역할(manager/staff/warehouse/viewer)에 반영.

---

## 5. 재고·원가·회계 흐름 요약

- **입고(발주 receive)**: IN 이동 생성 → 잔고 증가 + 이동평균 원가 갱신, 발주 received_qty 누적.
- **출고(수주 ship)**: OUT 이동 생성 → 잔고 차감(부족 시 400·롤백), COGS = 출고수량 × 이동평균원가.
- **재고평가액** = Σ(현재고 × 이동평균원가), **매출총이익** = 매출 − COGS.
- **미지급/미수** = 매입/매출 청구(이동에서 파생) − 결제(payments). 결제를 PO/SO에 귀속하면 문서별 미지급/미수도 산출.

---

## 6. 프론트엔드 콘솔 (`app/static/index.html`)

추가 화면(네비 + 섹션 + JS): **발주 · 수주 · 재고평가/손익 · 결제/미수미지급**.
- 발주/수주: 목록·상태전이·다중 라인 등록 모달·부분 입출고 모달·상세.
- 재고평가: 총평가액·매출총이익 카드 + 품목별 표.
- 결제: 거래처 잔액 조회 + 결제/수금 등록·목록. 문서 상세에서 "지급/수금 등록"(프리필) 연결.
- 기존 패턴(`api`/`openForm`/`renderPager`/`can`/모달) 재사용, 권한별 노출.

---

## 7. 신뢰성 (테스트 · 배포)

- **테스트**: `tests/`(conftest 격리 + `test_api.py`) — **12개** 회귀 테스트(인증/RBAC·부분수정·재고 가드·발주 입고·수주 출고+재고부족·이동평균/마진·결제/잔액·결제-문서 연결). `pytest.ini`, `requirements-dev.txt`.
- **Docker**: `Dockerfile`(alembic→seed→uvicorn), `docker-compose.yml`(MySQL8+API, 헬스체크·자동 마이그레이션), `.dockerignore`.
- **CI**: `.github/workflows/ci.yml` — push/PR마다 pytest.

### 검증 결과
- `pytest` → **12 passed**
- Alembic 6단계 `upgrade head` → 단일 head(`e5f6a7b8c9d0`)
- 인라인 JS `node --check` → 문법 OK
- **라이브 서버 스모크**(uvicorn 127.0.0.1:8000): 발주입고 30000/지급20000/미지급10000, 수주출고, 재고평가 18000, 매출총이익 6000, 공급처 미지급 10000 — 전부 일치 ✅

---

## 8. 실행 방법

```powershell
# 로컬 SQLite (즉시)
.\run_sqlite.ps1                      # 시드+서버 → http://localhost:8000 (admin/admin1234)

# MySQL 등 운영
alembic upgrade head
python -m app.seed
uvicorn app.main:app --port 8000

# Docker
docker compose up --build

# 테스트
pip install -r requirements-dev.txt
pytest
```

---

## 9. 보류 / 다음 후보

- **다중창고(warehouse)**: 재고 잔고를 `품목+창고` 차원으로 확장하는 코어 리팩터. 사용자 결정으로 이번 범위 제외.
- 결제 부분: 문서 부분결제 스케줄·만기(aging) 리포트.
- 프론트: 단일 HTML → Next.js SPA 전환(주 스택 정합), OpenAPI→TS 타입 자동생성.
- `stats.py`/`reports.py`가 신규 인덱스를 활용하도록 쿼리 점검.

---

## 10. 파일 트리 (핵심)

```
erp/
├─ app/
│  ├─ config.py            시크릿 가드
│  ├─ models.py            +StockBalance/PO/SO/Payment, 인덱스
│  ├─ schemas.py           +문서·원가·결제 스키마
│  ├─ services.py          apply_stock_delta / post_movement (원가 포함)
│  ├─ seed.py              +권한/역할
│  ├─ main.py              +라우터 등록, SQLite 한정 create_all
│  ├─ static/index.html    +발주/수주/재고평가/결제 화면
│  └─ api/
│     ├─ purchase.py  sales.py  costing.py  payments.py   (신규)
│     ├─ stock.py     (잔고 기반 재작성)
│     └─ partners.py  (+ /balance)
├─ migrations/             Alembic (env.py + versions/ 6개)
├─ tests/                  conftest.py + test_api.py (12)
├─ Dockerfile  docker-compose.yml  .dockerignore
├─ .github/workflows/ci.yml
├─ pytest.ini  requirements.txt  requirements-dev.txt
└─ 고도화_작업내역.md       (이 문서)
```
