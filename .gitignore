# === 보안: 절대 커밋 금지 ===
.env
.env.*
!.env.example
*.key
*.pem
*.p12
secrets.*

# === 데이터베이스 (실제 데이터 제외) ===
*.db
*.sqlite
*.sqlite3
erp_dev.db
# 스키마 복구·마이그레이션 전에 뜨는 백업본(erp_dev.db.bak-YYYYmmdd-HHMMSS 등).
# 확장자가 .db 로 끝나지 않아 위 *.db 에 안 걸린다 — 비밀번호 해시가 든 파일이다.
*.db.bak*
*.db-*
*.sqlite.bak*

# === Python ===
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# === OS / 에디터 ===
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# === graphify (코드 지도 산출물) ===
graphify-out/
